logging:
  level:
    org.springframework: error
    com.vts: warn
    liquibase: info

spring:
  main:
    allow-bean-definition-overriding: true
  jpa:
    hibernate:
      ddl-auto: none
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    url: jdbc:postgresql://smartgeosystem.ru:5455/vesseldb
    username: postgres
    password: password
    hikari:
      poolName: Hikari
      auto-commit: false
  liquibase:
    change-log: classpath:/db/liquibase/master.xml